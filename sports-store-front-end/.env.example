# Copy to .env and fill in real values. Never commit .env.
# Base URL of the backend API (trailing /api/v1 included, no trailing slash).
# Local dev backend runs on 5000 (or 5001 on macOS where AirPlay takes 5000).
VITE_API_URL=http://localhost:5000/api/v1
